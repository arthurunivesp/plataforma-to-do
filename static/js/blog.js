document.addEventListener('DOMContentLoaded', function() {
    console.log('Blog page loaded');

    // Função para carregar mais posts (futuramente)
    function loadMorePosts() {
        // Lógica para carregar mais posts via AJAX ou API
    }

    // Função para filtrar posts (futuramente)
    function filterPosts(category) {
        // Lógica para filtrar posts por categoria
    }
});
